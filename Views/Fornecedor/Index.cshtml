@model IEnumerable<Sistema_Buffer_BH.Models.FornecedorModel>

@{
    ViewData["Title"] = "Index";
}

<div class="d-flex justify-content-between align-items-center mt-2">
    <h1 class="ps-2">Fornecedores</h1>

    <a class="btn mx-5 icone" asp-controller="Fornecedor" asp-action="Create"><i class="bi bi-truck fs-3">+</i></a>

</div>

<hr />

<div class="container">

    <table id="Fornecedores" class="table table-dark table-striped mt-5 caption-top">
        <caption>
            <div class="row g-3">
                <div class="col-sm-7">
                    <h2>Lista de Produtos</h2>
                </div>
                <div class="col-sm">
                </div>
                <div class="col-sm">
                    <input type="text" id="filtro" class="form-control mb-2" placeholder="Pesquisar...">
                </div>
            </div>
        </caption>
        <thead>
            <tr>
                <th scope="col">Código</th>
                <th scope="col">Nome</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody class="table-group-divider" id="tabela">
            @if (Model != null)
            {

                @foreach (var fornecedores in Model)

                {
                    <tr>
                        <td scope="row">@fornecedores.Id</td>
                        <td scope="row">@fornecedores.Nome</td>
                        <td>
                            <!--EXCLUIR-->
                            <a class="btn text-white mx-2 float-end" asp-controller="Fornecedor" asp-action="Delete"
                               asp-route-id="@fornecedores.Id"><i class="bi bi-trash-fill"></i></a>
                            <!--EDITAR-->
                            <a class="btn text-white mx-2 float-end" asp-controller="Fornecedor" asp-action="Edit"
                               asp-route-id="@fornecedores.Id"><i class="bi bi-pencil"></i></a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

<!-- Paginação Bootstrap -->
<nav>
    <ul class="pagination justify-content-center" id="paginacao"></ul>
</nav>

<script>
       const linhasPorPagina = 6;
    let paginaAtual = 1;

    const linhas = Array.from(document.querySelectorAll("#tabela tr"));

    function mostrarPagina(pagina) {
        paginaAtual = pagina;

        const inicio = (pagina - 1) * linhasPorPagina;
        const fim = inicio + linhasPorPagina;

        linhas.forEach((linha, index) => {
            linha.style.display = (index >= inicio && index < fim) ? "" : "none";
        });

        renderizarPaginacao();
    }

    function renderizarPaginacao() {
        const totalPaginas = Math.ceil(linhas.length / linhasPorPagina);
        const paginacao = document.getElementById("paginacao");

        paginacao.innerHTML = "";

        for (let i = 1; i <= totalPaginas; i++) {
            let li = document.createElement("li");
            li.className = "page-item " + (i === paginaAtual ? "active" : "");

            let a = document.createElement("a");
            a.className = "page-link";
            a.innerText = i;
            a.href = "#";
            a.onclick = (e) => {
                e.preventDefault();
                mostrarPagina(i);
            };

            li.appendChild(a);
            paginacao.appendChild(li);
        }
    }


    // 🔍 Filtro funcionando junto
    document.getElementById("filtro").addEventListener("keyup", function () {
        const valor = this.value.toLowerCase();

        linhas.forEach(linha => {
            const texto = linha.textContent.toLowerCase();
            linha.style.display = texto.includes(valor) ? "" : "none";
        });

        // reinicia paginação após filtro
        paginaAtual = 1;
        renderizarPaginacao();
    });

    // inicialização
    mostrarPagina(1);
</script>